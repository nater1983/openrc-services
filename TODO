* Move or remove system specific fixes.
  The way we fix the services depends upon the system.
  So either make it system specific (ie, fixes.Slackware)
  or remove it from here (can then be done by system packager
  while packaging).
